import React from 'react';

const NotFound = () => {

    const style = {
        display: 'flex',
        justifyContent: 'center',
        alignContent: 'center',
        textAlign: 'center',
    }

    return (
        <div style={style}>
            You look lost.
            <br/>
            Follow the bread crumb trails by the pigeon highway dumbo
        </div>
    );
};

export default NotFound;